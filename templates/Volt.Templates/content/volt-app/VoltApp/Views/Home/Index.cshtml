@{
    ViewData["Title"] = "Welcome";
    Layout = null;
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>VoltApp</title>
    <style>
        * { box-sizing: border-box; margin: 0; padding: 0; }

        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
            background: #0a0a0a;
            color: #e0e0e0;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
        }

        .hero {
            text-align: center;
            padding: 2rem;
            max-width: 720px;
        }

        .logo {
            font-size: 5rem;
            margin-bottom: 0.25rem;
        }

        .brand {
            font-size: 3rem;
            font-weight: 700;
            letter-spacing: -0.03em;
            background: linear-gradient(135deg, #fbbf24, #f59e0b, #d97706);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .version {
            font-size: 0.875rem;
            color: #666;
            margin-bottom: 2rem;
        }

        .tagline {
            font-size: 1.25rem;
            color: #999;
            margin-bottom: 3rem;
            line-height: 1.6;
        }

        .cards {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 1rem;
            width: 100%;
            margin-bottom: 3rem;
        }

        .card {
            background: #141414;
            border: 1px solid #222;
            border-radius: 8px;
            padding: 1.25rem;
            text-align: left;
            transition: border-color 0.2s;
        }

        .card:hover { border-color: #d97706; }

        .card h3 {
            font-size: 0.875rem;
            font-weight: 600;
            color: #f59e0b;
            margin-bottom: 0.5rem;
        }

        .card p {
            font-size: 0.8125rem;
            color: #888;
            line-height: 1.5;
        }

        .card code {
            background: #1a1a1a;
            padding: 0.125rem 0.375rem;
            border-radius: 3px;
            font-size: 0.75rem;
            color: #d97706;
        }

        .getting-started {
            background: #141414;
            border: 1px solid #222;
            border-radius: 8px;
            padding: 1.5rem 2rem;
            width: 100%;
            margin-bottom: 2rem;
        }

        .getting-started h2 {
            font-size: 1rem;
            font-weight: 600;
            color: #ccc;
            margin-bottom: 1rem;
        }

        .step {
            display: flex;
            align-items: flex-start;
            gap: 0.75rem;
            margin-bottom: 0.75rem;
        }

        .step:last-child { margin-bottom: 0; }

        .step-num {
            background: #d97706;
            color: #0a0a0a;
            font-size: 0.75rem;
            font-weight: 700;
            width: 1.5rem;
            height: 1.5rem;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            flex-shrink: 0;
            margin-top: 0.125rem;
        }

        .step-text {
            font-size: 0.875rem;
            color: #aaa;
            line-height: 1.5;
        }

        .step-text code {
            background: #1a1a1a;
            padding: 0.125rem 0.375rem;
            border-radius: 3px;
            font-size: 0.8125rem;
            color: #f59e0b;
        }

        .footer {
            font-size: 0.75rem;
            color: #444;
        }

        .footer a {
            color: #666;
            text-decoration: none;
        }

        .footer a:hover { color: #d97706; }
    </style>
</head>
<body>
    <div class="hero">
        <div class="logo">&#9889;</div>
        <div class="brand">VoltApp</div>
        <div class="version">Volt Framework v0.2.0</div>
        <p class="tagline">
            You're running on Volt — a Rails-like framework for .NET.<br />
            This page is served from <code>Controllers/HomeController.cs</code> and <code>Views/Home/Index.cshtml</code>.
        </p>

        <div class="cards">
            <div class="card">
                <h3>Generate a Scaffold</h3>
                <p>Create a full CRUD resource with one command:</p>
                <p><code>volt generate scaffold Post title:string body:text</code></p>
            </div>
            <div class="card">
                <h3>Run Migrations</h3>
                <p>Apply database changes:</p>
                <p><code>volt db:migrate</code></p>
            </div>
            <div class="card">
                <h3>Convention Routing</h3>
                <p>Controllers in <code>Controllers/</code> are auto-discovered with RESTful routes.</p>
            </div>
        </div>

        <div class="getting-started">
            <h2>Getting Started</h2>
            <div class="step">
                <span class="step-num">1</span>
                <span class="step-text">Generate your first resource: <code>volt generate scaffold Article title:string body:text published:bool</code></span>
            </div>
            <div class="step">
                <span class="step-num">2</span>
                <span class="step-text">Run the migration: <code>volt db:migrate</code></span>
            </div>
            <div class="step">
                <span class="step-num">3</span>
                <span class="step-text">Browse to <code>/articles</code> and start building</span>
            </div>
            <div class="step">
                <span class="step-num">4</span>
                <span class="step-text">Replace this page by editing <code>Views/Home/Index.cshtml</code></span>
            </div>
        </div>

        <div class="footer">
            <a href="https://github.com/funcoder/volt">GitHub</a>
        </div>
    </div>
</body>
</html>
