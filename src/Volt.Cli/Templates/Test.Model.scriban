using Xunit;
using {{ model_namespace }};

namespace {{ test_namespace }}.Models;

/// <summary>
/// Unit tests for the <see cref="{{ model_name }}"/> model.
/// </summary>
public class {{ model_name }}Test
{
    [Fact]
    public void Can_Create_{{ model_name }}()
    {
        var model = new {{ model_name }}
        {
{{~ for field in fields ~}}
            {{ field.name }} = {{ field.default_value }},
{{~ end ~}}
        };

{{~ for field in fields ~}}
        Assert.Equal({{ field.default_value }}, model.{{ field.name }});
{{~ end ~}}
    }

    [Fact]
    public void Has_Timestamp_Properties()
    {
        var model = new {{ model_name }}();

        Assert.Equal(default, model.CreatedAt);
        Assert.Equal(default, model.UpdatedAt);
        Assert.Null(model.DeletedAt);
    }
}
